{{ template "base" .}}
 
{{define "content"}}

<div class="container">
    <div class="row">
        <div class="col">
            <h1>This the Home Page</h1>
            <p>Paragraph in home:</p>
        </div>
    </div>
</div>

{{end}}